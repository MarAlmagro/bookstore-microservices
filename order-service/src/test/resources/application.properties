# Test Configuration for Order Service
spring.data.mongodb.database=test_orders
spring.mongodb.embedded.version=4.0.21

# Catalog Service URL for tests (will be mocked)
catalog.service.url=http://localhost:8081

# Logging
logging.level.com.bookstore.order=DEBUG
logging.level.org.springframework.data.mongodb=DEBUG
